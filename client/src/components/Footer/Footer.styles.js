import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles(() => ({
  root: {
    width: '100%',
    backgroundColor: '#0b3954',
    marginTop: '150px',
    paddingTop: '40px',
    bottom: -30,
    position: 'absolute',
    color: 'white',
    height: '60px'
  }
}));